name: Placeholder — CodeQL Disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
      - run: echo "Default CodeQL setup is active. This workflow is intentionally empty."

