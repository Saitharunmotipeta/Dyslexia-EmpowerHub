name: Deploy Frontend

on:
  push:
    branches: [ saitharun ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - run: |
          cd frontend
          docker compose up -d --build