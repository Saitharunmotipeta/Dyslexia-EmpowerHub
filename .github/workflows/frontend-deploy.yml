name: Deploy Frontend

on:
  push:
    branches: [ prod ]
    paths:
      - "frontend/**"

jobs:
  deploy-frontend:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Build frontend
        run: |
          cd frontend
          docker compose up -d --build