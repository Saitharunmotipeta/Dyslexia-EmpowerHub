name: Deploy Backend

on:
  push:
    branches: [ prod ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - run: |
          cd backend
          docker compose up -d --build