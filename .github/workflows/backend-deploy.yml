name: Deploy Backend

on:
  push:
    branches: [ prod ]
    paths:
      - "backend/**"

jobs:
  deploy-backend:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Build backend
        run: |
          cd backend
          docker compose up -d --build